public class abiline implements Kalkulaator{
    public double ruutjuur(int a){
        return Math.sqrt(a);
    }
    public double ruut(int a){
        return Math.pow(a, 2.0);
    }
    public double exponent(int arvA, int arvE){
        return Math.pow(arvA, arvE);
    }
}
